import { ReactNode } from "react"

export const metadata = {
  title: "Randimalz | Credits",
  description: "Thank you!",
}

export default function CreditsLayout({ children }: { children: ReactNode }) {
  return children
}
