import { ReactNode } from "react"

export const metadata = {
  title: "Randimalz | Dog Facts",
  description: "Get random facts about dogs with pictures!",
}

export default function DogsLayout({ children }: { children: ReactNode }) {
  return children
}
