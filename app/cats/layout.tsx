import { ReactNode } from "react"

export const metadata = {
  title: "Randimalz | Cat Facts",
  description: "Get random facts about cats with pictures!",
}

export default function CatsLayout({ children }: { children: ReactNode }) {
  return children
}
